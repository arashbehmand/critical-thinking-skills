# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage

# Critical-thinking workspace artifacts (per-session receipts;
# force-add a receipt deliberately when it should be kept)
.ct/

# Agent-local config (the .claude/skills symlink IS tracked)
.claude/settings.local.json
CLAUDE.local.md
.mcp.json

# macOS
.DS_Store

# bench/ is local-only measurement for now: a seeded instance generator, its scorers, and
# the process-audit run artifacts. Kept out of the repo by request. tests/test_bench.py
# imports it, so that travels with it or a fresh clone cannot collect the suite.
bench/
tests/test_bench.py
